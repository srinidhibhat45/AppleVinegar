## What this changes

## Why

## Screenshots
<!-- Required for anything visual. AppleCider is a visual tool; a screenshot is the diff
     that matters. Before/after if you changed something that already existed. -->

## Checklist
- [ ] `npm run build` passes
- [ ] Document mutations go through the store (`mutate` / `begin` + `end`)
- [ ] New wireframe visuals use `--w-*` tokens and look right in Sketch, Wire and Mono
- [ ] No new runtime dependency (or the PR explains why one is needed)
